import { RPEditor } from "@/components";
import { FC } from "react";
import "../globals.css";

export const ReditPage: FC = () => {
  return (
    <div className="w-full h-screen">
      <RPEditor />
    </div>
  );
};
